spring:
  application:
    name: auth-service

  config:
    import: optional:configserver:http://config-server:8888
    fail-fast: false
    retry:
      enabled: true
      initial-interval: 2000
      max-interval: 10000
      max-attempts: 10